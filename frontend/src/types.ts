import { ReactNode } from "react";

export interface HeadingProps {
    title: string
    isTextWhite: boolean
}

export interface BlurryBackgroundProps {
    title: string;
    content: ReactNode;
}
